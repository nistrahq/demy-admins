package com.nistra.demy.admins.core.data.remote

import retrofit2.http.GET

/**
 * This is a placeholder API service interface.
 * Replace the example endpoint with your actual API endpoints in feature folders.
 * @author Salim Ramirez
 */
interface ApiService {
    // @GET("items")
    // suspend fun getItems(): List<ItemDto>
}
